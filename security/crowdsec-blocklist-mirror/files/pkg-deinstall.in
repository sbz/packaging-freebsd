#!/bin/sh

case $2 in
       "DEINSTALL")
               service crowdsec_mirror status && touch /var/run/crowdsec_mirror.running
               service crowdsec_mirror stop || :
               ;;
esac

